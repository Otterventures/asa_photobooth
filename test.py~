import time

import RPi.GPIO as GPIO
GPIO.setmode(GPIO.BCM)

GPIO.setup(3,GPIO.IN)

btnState = False

while True:
    if (not GPIO.input(3) and btnState == False):
        print "button pressed"
        btnState = True;
